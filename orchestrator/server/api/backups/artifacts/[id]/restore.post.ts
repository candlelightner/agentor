defineRouteMeta({openAPI:{tags:['Backups'],summary:'Restore encrypted backup',operationId:'restoreBackup',parameters:[{name:'id',in:'path',required:true,schema:{type:'string'}}],requestBody:{required:false,content:{'application/json':{schema:{type:'object',properties:{mode:{type:'string',enum:['new','original']},displayName:{type:'string'}}}}}},responses:{201:{description:'Restored new worker'},400:{description:'Unsafe or invalid restore'},401:{description:'Unauthorized'},403:{description:'Forbidden'},404:{description:'Not found'}}}});
import {requireAuth} from '../../../../utils/auth-helpers';import {useBackupManager} from '../../../../utils/backup-manager';export default defineEventHandler(async(event)=>{const {user}=requireAuth(event);const manager=useBackupManager();const artifact=await manager.getArtifact(getRouterParam(event,'id')!);if(!artifact)throw createError({statusCode:404,statusMessage:'Backup artifact not found'});if(user.role!=='admin'&&artifact.userId!==user.id)throw createError({statusCode:403,statusMessage:'Forbidden'});const body:{mode?:'new'|'original';displayName?:string}=await readBody<{mode?:'new'|'original';displayName?:string}>(event).catch(()=>({}));try{const worker=await manager.restore(artifact.userId,artifact,body.mode??'new',body.displayName);setResponseStatus(event,201);return worker;}catch(err){throw createError({statusCode:400,statusMessage:err instanceof Error?err.message:'Restore failed'});}});
