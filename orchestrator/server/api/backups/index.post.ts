defineRouteMeta({openAPI:{tags:['Backups'],summary:'Start backup',operationId:'startBackup',requestBody:{required:true,content:{'application/json':{schema:{type:'object'}}}},responses:{202:{description:'Queued'},400:{description:'Invalid selection'},401:{description:'Unauthorized'}}}});import{requireAuth}from'../../utils/auth-helpers';import{useBackupManager}from'../../utils/backup-manager';import{useWorkerStore}from'../../utils/services';import type{BackupProviderKind}from'../../utils/backup-types';export default defineEventHandler(async e=>{const u=requireAuth(e).user;const b=await readBody<{workspaceIds?:string[];selection?:string;providerId?:BackupProviderKind}>(e);const ids=b?.selection==='all'?useWorkerStore().list().filter(w=>w.userId===u.id).map(w=>w.id):b?.workspaceIds;if(!ids?.length)throw createError({statusCode:400,statusMessage:'workspaceIds or all selection is required'});try{const j=await useBackupManager().createMany(u.id,ids,b.providerId);setResponseStatus(e,202);return j;}catch(err){throw createError({statusCode:400,statusMessage:err instanceof Error?err.message:'Backup request failed'});}});
