defineRouteMeta({openAPI:{tags:['Backups'],summary:'Delete backup',operationId:'deleteBackup',responses:{204:{description:'Deleted'},401:{description:'Unauthorized'},403:{description:'Forbidden'},404:{description:'Not found'}}}});import{requireAuth}from'../../utils/auth-helpers';import{useBackupManager}from'../../utils/backup-manager';export default defineEventHandler(async e=>{const u=requireAuth(e).user,m=useBackupManager(),a=await m.getArtifact(getRouterParam(e,'id')!);if(!a)throw createError({statusCode:404,statusMessage:'Backup not found'});if(u.role!=='admin'&&a.userId!==u.id)throw createError({statusCode:403,statusMessage:'Forbidden'});await m.deleteArtifact(a);setResponseStatus(e,204);return null;});
