defineRouteMeta({openAPI:{tags:['Managed networks'],summary:'Delete managed network',operationId:'deleteManagedNetwork'}});import {requireResourceAccess} from '../../utils/auth-helpers';import {useManagedNetworkStore} from '../../utils/services';import {useManagedNetworkManager} from '../../utils/managed-network-manager';export default defineEventHandler(async e=>{const id=getRouterParam(e,'id')!,s=useManagedNetworkStore(),n=s.findById(id);requireResourceAccess(e,n,{allowGlobal:false});await useManagedNetworkManager().remove(n!);await s.remove(n!.userId,id);setResponseStatus(e,204);return null});
