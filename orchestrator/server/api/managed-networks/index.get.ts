defineRouteMeta({openAPI:{tags:['Managed networks'],summary:'List managed networks',operationId:'listManagedNetworks'}}); import {requireAuth} from '../../utils/auth-helpers';import {useManagedNetworkStore} from '../../utils/services'; export default defineEventHandler(e=>{const {user}=requireAuth(e);return user.role==='admin'?useManagedNetworkStore().list():useManagedNetworkStore().listForUser(user.id)});
