defineRouteMeta({openAPI:{tags:['Managed networks'],summary:'Inspect managed network topology',operationId:'getManagedNetwork'}});import {requireResourceAccess} from '../../utils/auth-helpers';import {useManagedNetworkStore} from '../../utils/services';import {useManagedNetworkManager} from '../../utils/managed-network-manager';export default defineEventHandler(async e=>{const n=useManagedNetworkStore().findById(getRouterParam(e,'id')!);requireResourceAccess(e,n,{allowGlobal:false});return useManagedNetworkManager().topology(n!)});
