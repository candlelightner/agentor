defineRouteMeta({openAPI:{tags:['Backups'],summary:'List backup providers',operationId:'listBackupProviders',responses:{200:{description:'Redacted providers'},401:{description:'Unauthorized'}}}});import{requireAuth}from'../../utils/auth-helpers';import{useBackupManager}from'../../utils/backup-manager';export default defineEventHandler(e=>useBackupManager().providersStatus(requireAuth(e).user.id));
