defineRouteMeta({ openAPI: { tags: ['Worker groups'], summary: 'Create worker group', operationId: 'createWorkerGroup', responses: { 201: { description: 'Created group' }, 400: { description: 'Invalid name' } } } });
import { requireAuth } from '../../utils/auth-helpers'; import { useWorkerGroupStore } from '../../utils/services';
import { withWorkerNetworkMutation } from '../../utils/worker-group-manager';
import { WorkerGroupHierarchy } from '../../utils/worker-group-hierarchy';
export default defineEventHandler(async (event) => { const { user } = requireAuth(event); const { name, parentId } = await readBody(event); if (typeof name !== 'string' || !name.trim() || name.trim().length > 100) throw createError({ statusCode: 400, statusMessage: 'A group name of at most 100 characters is required' }); if(parentId!==undefined&&parentId!==null&&typeof parentId!=='string')throw createError({statusCode:400,statusMessage:'parentId must be a string or null'}); return withWorkerNetworkMutation(user.id,async()=>{const store=useWorkerGroupStore();new WorkerGroupHierarchy(store).validateParent(user.id,undefined,parentId);setResponseStatus(event, 201); return store.create(user.id, name,parentId||undefined);}); });
