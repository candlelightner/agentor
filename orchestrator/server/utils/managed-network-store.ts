import { randomUUID } from 'node:crypto'; import { UserScopedJsonStore } from './user-scoped-store';
export interface ManagedNetwork { id:string; userId:string; name:string; dockerName:string; scope:'selected'|'group'; groupId?:string; workerIds:string[]; createdAt:string; updatedAt:string; }
export class ManagedNetworkStore extends UserScopedJsonStore<string,ManagedNetwork> { constructor(dataDir:string){super(dataDir,'managed-networks.json',x=>x.id)} findById(id:string){return this.findWithOwner(x=>x.id===id)?.item} async create(userId:string,name:string,scope:'selected'|'group',groupId?:string){const now=new Date().toISOString(),id=randomUUID(),item={id,userId,name:name.trim(),dockerName:`agentor-managed-${id}`,scope,groupId,workerIds:[],createdAt:now,updatedAt:now};await this.setItem(userId,item);return item} async update(userId:string,id:string,patch:Partial<Pick<ManagedNetwork,'name'|'scope'|'groupId'|'workerIds'>>){const item=this.get(userId,id);if(!item)throw createError({statusCode:404,statusMessage:'Managed network not found'});Object.assign(item,patch,{workerIds:patch.workerIds?[...new Set(patch.workerIds)]:item.workerIds,updatedAt:new Date().toISOString()});await this.setItem(userId,item);return item} async remove(userId:string,id:string){if(!await this.deleteItem(userId,id))throw createError({statusCode:404,statusMessage:'Managed network not found'})} }
